const { resolve } = require('path');

module.exports = {
  config: resolve('config/config.ts'),
  'seeders-path': resolve('seeders'),
  'migrations-path': resolve('migrations'),
  'models-path': resolve('src/models')
};